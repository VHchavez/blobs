package:
    name: blobs

channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pyplot
  - numpy
  - conda

    # Testing
  - pytest
  - pytest-cov
  - codecov

requirements:
    build:
        - python
        - numpy

    # Pip-only installs
  #- pip:
  #  - codecov

